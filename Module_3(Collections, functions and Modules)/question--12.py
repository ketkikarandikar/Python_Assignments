# Write a Python program to convert a list of characters into a string.

def convert(k):  
    str = ""   # blank string

    return(str.join(k))   # To join the string in a single word

k = input("Enter string : ")    

print(convert(k))    # print the whole word